# 렌더 슬립 방어 코드

렌더 무료 플랜은 15분 동안 요청이 없으면 서버가 잠듭니다.

잠든 서버는 첫 요청에서 30~60초를 그냥 기다리게 만듭니다.

이번에 그 상황을 견디는 코드를 채웠습니다.


## 1. 이미 있던 것

지난 작업에서 들어가 있던 부분입니다.

| 항목 | 위치 |
|---|---|
| 제한 시간 fetch | App.jsx |
| 재시도 | App.jsx |
| 깨우는 중 안내 | App.jsx |
| /health 엔드포인트 | main.py |
| 백엔드 재시도 | main.py |


## 2. 이번에 더한 것

| 항목 | 파일 |
|---|---|
| AI 요청 제한 시간 분리 | App.jsx |
| 중복 전송 방지 | App.jsx |
| keep-alive 두드리기 | App.jsx |
| 탭 복귀 시 두드리기 | App.jsx |
| API 주소 환경변수 | App.jsx |
| 대기 예산 맞추기 | main.py |
| 루트(/) 응답 | main.py |


## 3. 왜 고쳤나

### 3-1. AI 요청 제한 시간

전에는 모든 요청이 20초에서 끊겼습니다.

그런데 백엔드는 AI를 최대 60초까지 기다렸습니다.

화면이 먼저 포기하는 구조였습니다.

이제 AI 요청만 100초를 기다립니다.

상수 이름은 `AI_TIMEOUT` 입니다.


### 3-2. 중복 전송 방지

메시지 전송은 DB에 글을 쓰는 요청입니다.

끊겼다고 다시 보내면 같은 말이 두 번 저장됩니다.

그래서 재시도를 껐습니다.

대신 끊기면 목록을 다시 읽습니다.

이미 저장돼 있으면 다시 보내지 않습니다.

함수 이름은 `isSaved` 입니다.


### 3-3. keep-alive

화면이 열려 있는 동안 10분마다 `/health`를 두드립니다.

슬립에 빠지는 것 자체를 막습니다.

숨겨진 탭에서는 두드리지 않습니다.

다른 탭에 갔다 돌아오면 곧바로 한 번 두드립니다.


### 3-4. 대기 예산

백엔드를 40초 x 2회 = 80초로 줄였습니다.

프런트의 100초보다 짧습니다.

이제 서버가 보낸 오류 문구를 화면이 받아 볼 수 있습니다.

전에는 백엔드가 최대 180초까지 매달렸습니다.


### 3-5. API 주소

전에는 `http://127.0.0.1:8000`이 코드에 박혀 있었습니다.

배포본이 자기 컴퓨터를 바라보는 상태였습니다.

이제 `VITE_API_URL` 값을 먼저 봅니다.

값이 없으면 로컬 주소를 씁니다.


## 4. 배포할 때 할 일

프런트를 빌드하기 전에 환경변수를 넣습니다.

`frontend/.env.production` 파일을 만듭니다.

```
VITE_API_URL=https://chatbot-db-back-f4ut.onrender.com
```

그다음 빌드합니다.

```
npm run build
```


## 5. 남은 문제

이건 코드로 못 막습니다.

렌더 무료 플랜은 디스크가 임시입니다.

서버가 잠들거나 재시작하면 `chat.db`가 지워집니다.

대화 기록이 통째로 날아갑니다.

해결하려면 외부 DB가 필요합니다.

무료로는 Supabase, Neon 같은 곳이 있습니다.

이 부분을 진행할지는 따로 정해 주세요.


## 6. 검사 결과

| 검사 | 결과 |
|---|---|
| main.py 문법 | 통과 |
| App.jsx ESLint | 오류 0 |

경고 1건이 남아 있습니다.

`useEffect` 의존성 경고입니다.

이번 수정 전부터 있던 것이고 동작에는 문제가 없습니다.

실제 서버에 올려서 돌려보지는 않았습니다.
